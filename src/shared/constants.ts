// Game configuration constants
export const GAME_DURATION_SECONDS = 60;
export const GAME_DURATION_MS = GAME_DURATION_SECONDS * 1000;

// Warning threshold for low time (in seconds)
export const LOW_TIME_THRESHOLD = 10;

// Daily Challenge constants
export const DAILY_CHALLENGE_PROBLEMS_COUNT = 5;

// Developer testing flag: when true, allows replaying the daily challenge multiple
// times for testing. Keep false for production.
export const ALLOW_DAILY_RETRIES_FOR_TESTING = false;
